{
  "name": "stopwatches",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "react-dom": "latest",
    "react": "latest"
  },
  "keywords": [
    "react"
  ],
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}